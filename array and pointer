#include <stdio.h>

int main () {
    int arr[3] = {1, 2, 3};
    int *ptr_arr = arr;

    printf("Nama variabel: arr\n");
    printf("isi variabel : \n");
    for (int i = 0; i < 3; i++) {
        printf("%d ", *ptr_arr);
        ptr_arr++;
    }
}

void swap(int *a, int *b) {
    int temp = *a;
    *a = *b;
    *b = temp;
}

int main () {
    int x = 3, y = 9;

    printf("Sebelum swap: x = %d, y = %d\n", x, y);
    swap(&x, &y);
    printf("Setelah swap: x = %d, y = %d\n", x, y);

    return 0;
}
